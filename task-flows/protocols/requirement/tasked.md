# Stage: tasked

All leaf requirements have linked implementation tasks.

## Who acts
System (automatic) or lead.

## What to do
1. Verify every leaf requirement folder has at least one linked task in DB
2. Verify no orphan requirements (folders with no tasks)
3. Advance to `in_progress` when all tasks are assigned

## Exit gate
All leaves have DB tasks. Tasks are ready for assignment.
