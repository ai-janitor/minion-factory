# CHECKLIST: coder-blid-01 — Tasks 200 & 201
## Feature: Dashboard — show originating backlog ID in activity and task views

Tally: 0/4

- [ ] Read CLAUDE.md (done implicitly — worktree inherits main repo CLAUDE.md)
- [ ] Implement task 200: add `requirement_id` to `fetch_tasks()` and `get_task_pipeline()` in `queries.py`; expand `fetch_task_to_backlog_map()` call in `web_server.py`
- [ ] Implement task 201: add `.bkl-badge` CSS, BKL column in `renderTasks()`, BKL tag in activity feed in `index.html`
- [ ] Verify tests pass
